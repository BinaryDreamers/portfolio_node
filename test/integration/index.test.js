const request = require("supertest");
let server;

describe("Check all endpoints", () => {
  // beforeEach(() => (server = require("../index")));
  // afterEach(() => {
  //   server.close();
  // });
  it("", () => {});
});
